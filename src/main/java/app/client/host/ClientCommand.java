package app.client.host;

public enum ClientCommand {

    CONNECT,
    CLIENT_FILE_LIST,
    CLIENT_FILE_INFO,
    HANDLE_PUSH,
    HANDLE_PUSH_PACK,
    PUSH_ON_DEMAND,
    RE_PUSH,
    RE_PULL,
    HANDLE_RE_PUSH,
    CHECK_SENDING,
    CREATE_PART_OF_FILE
}
